# REVALO24

PropTech SPA (public marketplace + agent panel). Application code lives in `frontend/`.

```bash
cd frontend
npm install
npm run dev
```

GitHub Pages deploys from `main` via `.github/workflows/deploy-pages.yml`.
